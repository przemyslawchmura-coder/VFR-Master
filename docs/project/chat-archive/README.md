# Chat archive (non-authoritative)

Optional manually exported ChatGPT/Codex conversations may be stored here using names such as `YYYY-MM-DD-topic.md`, `.txt`, `.json`, or `.html`.

Chat archive is **NON-AUTHORITATIVE HISTORICAL CONTEXT**. Chat text is never production truth, technical evidence, source verification, or current implementation truth. Reconcile any useful claim with executable code, tests, source documents and git history before updating project memory.
