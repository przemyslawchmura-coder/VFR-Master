# Technical Research Factory Foundation #1 — 2026-09-01

Starting state: clean `main`, HEAD `86219e77a156dec2a5b29d0f42d690aa1a92720d`, origin/main `97484bf004c466a6f26a5e42ae07b91214e95962`, ahead 8/behind 0.

Implemented contract version 1 under `research/factory/`: manufacturer-neutral ResearchTarget, SourceProspect, ApplicabilityScope and GapPlan validators; explicit publication/access/authentication states; deterministic serialization; a dimension-preserving fail-closed applicability evaluator; the canonical ADR-012 readiness gate; GapPlan generation using the existing 44-field Service Core; and non-mutating adapters for current target, prospect and acquired-source shapes.

Existing repository states prove the layer is used: MT-09 B7N/LIT remains `ACCESS-BLOCKED`; Harley `94001064` becomes `REJECTED-MISMATCH` for MY2022; authenticated Yamaha and Honda owner-manual shapes map without losing ABS/transmission/equipment uncertainty; and Ténéré `BW3-F8197-E0` remains `REGISTERED-NOT-REAUTHENTICATED` / `FACTORY-PILOT-CANDIDATE`.

Audit: `ACCEPT-WITH-RISKS`. The core is generic, pure, JSON-safe and fail-closed, but historical readiness/applicability implementations remain unchanged behind adapters until a bounded migration. No orchestrator, external research, source access, evidence, researched-no-evidence addition, coverage change, production/runtime/catalogue/cloud/release change or Ténéré authentication occurred.

Independent falsification checks: the contracts contain no manufacturer-specific core fields, and current Honda, Yamaha, Harley-Davidson and Ténéré records traverse adapters into the canonical evaluator rather than serving only as parallel-schema snapshots. Explicit tests distinguish ABS `false`, `true` and `UNKNOWN`; manual from DCT; EU from US; standard from SP; exact/range/unknown years; and `UNKNOWN`/`PARTIAL` from `MATCH`. Unknown or partial scope, blocked/broken/mirror-only access, unresolved publication identity, exhaustion and deterministic mismatch classifications all prevent execution readiness. GapPlan reuses exactly 44 canonical Service Core fields, preserves explicit researched-no-evidence and conflict states, and does not manufacture either from a missing field. Repeated validation/serialization is deterministic for canonical records, JSON round trips preserve literal `false`, and adapters/validators leave their input fixtures byte-equivalent. The full base-to-working-tree path audit found no historical-data migration, research evidence, production import, Technical Profile/source registry, runtime/browser, catalogue, Supabase, release or VFR800 change; the Ténéré prospect remains registered but unauthenticated.

Expectation classification: the 13 added test cases and the project-state total change from 441 to 454 are `JUSTIFIED` because they add Foundation contract/gate/adapter and memory invariants without changing pre-existing behavior. No historical expectation was changed to accommodate new output. The unresolved risks are the retained duplicate legacy readiness/applicability logic, the more permissive omitted-dimension behavior in legacy `scopeMatches`, explicit-option requirements for ambiguous legacy text, and the absence of all-shape live migration proof. Those risks are bounded by non-production isolation and compatibility adapters and therefore warrant `ACCEPT-WITH-RISKS`, not `ACCEPT`; no material defect warrants `REJECT`.

Exact NEXT: bounded Technical Research Factory Orchestrator Foundation—stable Batch/TargetWork/SourceWorkItem/Attempt/Event identities, append-only events, deterministic reducer/snapshots, budgets, terminal-state enforcement and checkpoint/resume tests, with no external research or production change.
