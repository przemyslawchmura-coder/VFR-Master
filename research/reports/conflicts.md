# Research conflicts

> **NON-PRODUCTION RESEARCH DATA**

No conflicting value pair is currently recorded in the seed dataset. The schema and validator require at least two preserved candidates for every `conflicting` conflict group and never auto-select a winner.
