// NON-PRODUCTION architecture design. No motorcycle evidence or executable factory state.
"use strict";

const genericityStates = Object.freeze([
  "GENERIC-READY", "GENERIC-BUT-MANUAL-ORCHESTRATION", "PARTIALLY-GENERIC",
  "MANUFACTURER-SPECIFIC", "MODEL-SPECIFIC", "DESIGN-ONLY", "MISSING"
]);
const readinessStates = Object.freeze(["READY", "PARTIAL", "DESIGN-ONLY", "MISSING", "BLOCKED"]);
const decisionClasses = Object.freeze(["MUST-REMAIN-HUMAN", "CAN-BE-DETERMINISTIC", "CAN-BE-POLICY-DRIVEN", "CAN-BE-HEURISTIC-WITH-HUMAN-REVIEW", "UNSAFE-TO-AUTOMATE"]);

const pipelineStages = Object.freeze([
  stage("catalogue-target-generation", "catalogue variants + selection policy", "coarse batch targets", "research/lib/batch-research-pipeline.js#generateTargets", "PARTIALLY-GENERIC", true, "Deterministic catalogue traversal exists, but exact year/market/transmission/ABS/equipment target scope is supplied by handcrafted batch data."),
  stage("service-core-gap-planning", "target + evidence", "44 field status rows", "research/lib/batch-research-pipeline.js#calculateGaps", "GENERIC-READY", true, "Shared Service Core and four gap states are deterministic; safety/source relevance and prior attempts are not yet combined into GapPlan."),
  stage("source-discovery", "target + gaps + policy", "ranked source prospects", null, "MISSING", false, "Queries, manufacturer paths and prospect creation are prompt-orchestrated."),
  stage("source-registration", "external findings", "source/prospect records", "research/data/sources/*; research/data/source-prospect-authentication-quality-reassessment.js", "PARTIALLY-GENERIC", false, "Source and prospect records exist in several incompatible shapes without one validator or stable attempt history."),
  stage("source-authentication", "source prospect + metadata findings", "readiness classification + blockers", "research/data/source-prospect-authentication-quality-reassessment.js#evaluateReadinessGate", "GENERIC-BUT-MANUAL-ORCHESTRATION", true, "All-fields gate is deterministic but embedded in a reassessment dataset and fed by manually authored booleans."),
  stage("applicability-gate", "target scope + source/evidence scope", "applicable, blocked, ambiguous or mismatch", "research/lib/batch-research-pipeline.js#validateApplicability; js/research/technical-research-pipeline.js#scopeMatches", "PARTIALLY-GENERIC", true, "Fail-closed pieces exist, but representations differ and neither contract covers every model/year/market/transmission/ABS/equipment dimension consistently."),
  stage("acquisition", "ready prospect + budget", "source attempt + document/access result", null, "DESIGN-ONLY", false, "Disposition vocabulary exists; each execution constructs source attempts and budgets by hand."),
  stage("extraction", "authenticated document + GapPlan", "EvidenceCandidate[]", "research/lib/batch-research-pipeline.js#validateExtractionCandidate; js/research/technical-research-pipeline.js#validateCandidates", "PARTIALLY-GENERIC", true, "Candidate validation is reusable; candidate construction and promotion into evidence remain manual and two schemas coexist."),
  stage("normalization", "raw candidate value/unit", "preserved raw + normalized value/unit", "research/lib/batch-research-pipeline.js#normalizeValue", "PARTIALLY-GENERIC", true, "Only ml/L, psi/bar/kPa and N·m numeric conversions are covered; intervals, clearances, part numbers and action semantics are not canonicalized."),
  stage("conflict-detection", "verified scoped evidence", "conflict groups", "research/lib/batch-research-pipeline.js#detectConflicts; js/research/technical-research-pipeline.js#composeLayers", "GENERIC-READY", true, "Conflicts are preserved and never auto-selected; richer normalized-match/applicability-split classification is still needed."),
  stage("coverage-yield", "targets + evidence + documents", "coverage and yield metrics", "research/lib/batch-research-pipeline.js#buildBatchReport", "PARTIALLY-GENERIC", true, "Coverage is generic; practical/generic yield, document budgets and per-document yield are recomputed in handcrafted batch result modules."),
  stage("review-queue", "targets/gaps/conflicts", "ordered review items", "research/lib/batch-research-pipeline.js#buildReviewQueue; js/research/technical-research-pipeline.js#generateResearchQueue", "PARTIALLY-GENERIC", true, "Stable queues exist but lack canonical typed reasons, source work items, ownership and resolution state."),
  stage("checkpoint-resume", "batch state + events", "restartable snapshot", null, "MISSING", false, "Repository commits preserve history, but no stable batch/attempt/work-item state machine resumes without handcrafted reconstruction."),
  stage("human-review", "typed review items", "recorded decisions", null, "DESIGN-ONLY", false, "Human review is governed but decisions have no generic machine-readable contract."),
  stage("manual-production-promotion", "accepted research packet", "explicit production change task", "js/research/technical-research-pipeline.js#generateDraftProposal; production validators", "GENERIC-BUT-MANUAL-ORCHESTRATION", true, "Boundary is intentionally manual and tested; it must remain outside the factory orchestrator.")
]);

function stage(id, input, output, implementation, classification, deterministic, finding) {
  return Object.freeze({ id, input, output, schema: implementation ? "existing module-specific shape" : null, implementation, validators: implementation ? "existing tests/module checks" : null, classification, deterministic, reusableInBatch: ["GENERIC-READY", "PARTIALLY-GENERIC"].includes(classification), finding });
}

const componentInventory = Object.freeze({
  genericReady: Object.freeze(["canonical 183-field coverage schema", "canonical 44-field Service Core", "document identity and mirror deduplication", "proof-status filtering", "basic Service Core gap calculation", "verified-evidence conflict preservation", "deterministic report hashing", "production/research isolation tests"]),
  genericButManualOrchestration: Object.freeze(["ADR-012 readiness gate inputs", "research candidate validation", "production draft boundary", "source tier policy", "source-yield threshold interpretation"]),
  partiallyGeneric: Object.freeze(["catalogue target generation", "source/prospect registries", "applicability evaluation", "normalization", "coverage/yield reporting", "review queues", "completion/readiness vocabulary"]),
  manufacturerSpecific: Object.freeze(["Yamaha B7N/LIT and owner-manual path knowledge", "Honda Motopub/Helm and common-manual family knowledge", "Harley SIP publication routing", "manufacturer-specific discovery path hints embedded in reports/prompts"]),
  modelSpecific: Object.freeze(["Honda five-target pilot definitions/results", "Yamaha two-target batch definitions/results", "Harley single-target execution", "MT-09 prospect authentication/reconciliation modules", "VFR proof matrix and manual fingerprint"]),
  designOnly: Object.freeze(["source discovery interface", "acquisition work items", "budget state transitions", "typed review decisions", "factory completion state", "manufacturer adapter interface"]),
  missing: Object.freeze(["canonical ResearchTarget validator", "canonical SourceProspect validator/registry", "canonical ApplicabilityScope", "GapPlan combining history/risk/source relevance", "persistent Batch/WorkItem/Attempt/Event identities", "resume reducer/checkpoint", "generic source-work scheduler", "canonical yield calculator", "generic anti-loop reducer"])
});

const manualDecisions = Object.freeze([
  decision("target-scope-definition", "CAN-BE-POLICY-DRIVEN", "Catalogue identity can seed targets, but exact year/market/transmission/ABS/equipment policies must be explicit and reviewable."),
  decision("external-source-discovery", "CAN-BE-HEURISTIC-WITH-HUMAN-REVIEW", "Agents/adapters may propose sources; authority and applicability cannot be inferred."),
  decision("source-query-construction", "CAN-BE-HEURISTIC-WITH-HUMAN-REVIEW", "Manufacturer adapters may emit bounded query hints, never evidence or readiness decisions."),
  decision("document-deduplication", "CAN-BE-DETERMINISTIC", "Publication identity/content hash already supports deterministic deduplication."),
  decision("readiness-gate-evaluation", "CAN-BE-DETERMINISTIC", "Once findings are recorded, ADR-012 is an all-fields gate."),
  decision("applicability-acceptance", "CAN-BE-DETERMINISTIC", "Exact scope matches and UNKNOWN blocking are policy; ambiguous external facts become review items."),
  decision("source-priority-ranking", "CAN-BE-POLICY-DRIVEN", "Authority, marginal gap relevance, access, duplication, risk and prior yield are explicit inputs."),
  decision("technical-value-extraction", "CAN-BE-HEURISTIC-WITH-HUMAN-REVIEW", "Tools/agents may propose candidates; safety-critical candidates need independent validation."),
  decision("normalization", "CAN-BE-DETERMINISTIC", "Only registered conversions/parsers may normalize; unknown forms remain raw."),
  decision("conflict-resolution", "MUST-REMAIN-HUMAN", "Detection is deterministic, but selecting among applicable authoritative disagreements requires judgment/new evidence."),
  decision("safety-critical-acceptance", "MUST-REMAIN-HUMAN", "Automation may block or queue, never waive unresolved applicability/conflicts."),
  decision("budget-stop", "CAN-BE-POLICY-DRIVEN", "Configured per batch/target and enforced deterministically."),
  decision("exhaustion-and-anti-loop", "CAN-BE-DETERMINISTIC", "Attempt identities, repeated blockers and configured limits can generate terminal stop reasons."),
  decision("production-promotion", "MUST-REMAIN-HUMAN", "Existing governance requires a separate deliberate task and validator review."),
  decision("unsupported-inference", "UNSAFE-TO-AUTOMATE", "Platform/model similarity must never create scope or values.")
]);
function decision(id, classification, rationale) { return Object.freeze({ id, classification, rationale }); }

const applicabilityDimensions = Object.freeze(["catalogVariantKey", "generation", "years", "markets", "transmission", "abs", "equipment"]);
const contracts = Object.freeze({
  ResearchTarget: Object.freeze({ identity: Object.freeze(["targetId", "catalogVariantKey", "scopeId"]), required: Object.freeze(["catalogVariantKey", "scope", "sourcePriorityPolicyId", "serviceCoreBaseline", "researchHistoryRefs", "riskFlags"]), derived: Object.freeze(["manufacturer", "family", "generation", "catalogueYears", "remainingGaps"]), scope: applicabilityDimensions, rule: "Reference canonical catalogue identity; store only the bounded research scope and never duplicate mutable catalogue facts." }),
  SourceProspect: Object.freeze({ identity: Object.freeze(["prospectId", "targetId", "documentIdentityHint"]), required: Object.freeze(["manufacturer", "documentClass", "publisherAuthority", "publicationIdentifiers", "identifierRelationships", "officialLocations", "accessibility", "applicability", "sourceTier", "authentication", "exhaustion", "attemptRefs", "expectedMarginalGapClass", "readinessClassification", "blockers", "nextAction"]), rule: "Manufacturer adapters may add opaque metadata under adapterData; core gate fields remain manufacturer-neutral." }),
  ApplicabilityScope: Object.freeze({ dimensions: applicabilityDimensions, unknownSemantics: "UNKNOWN blocks applicability; null is never universal; abs false remains distinct from null", decisionStates: Object.freeze(["APPLICABLE", "NOT-APPLICABLE", "AMBIGUOUS", "MISMATCH"]), rule: "Every dimension is evaluated independently and fail-closed before source or evidence acceptance." }),
  GapPlan: Object.freeze({ required: Object.freeze(["targetId", "serviceCoreVersion", "startingCoverage", "remainingFields", "safetyCriticalFields", "researchedNoEvidenceFields", "conflictingFields", "sourceClassRelevance", "attemptedSourceClasses", "expectedMarginalOpportunity"]), rule: "Generated from canonical evidence/history; answers what remains without prompt-authored field lists." }),
  SourceWorkItem: Object.freeze({ required: Object.freeze(["workItemId", "batchId", "targetId", "prospectId", "stage", "status", "budgetSnapshot", "inputRefs", "attemptIds", "reviewItemIds"]), statuses: Object.freeze(["PENDING", "IN-PROGRESS", "BLOCKED", "COMPLETED", "EXHAUSTED", "REVIEW-REQUIRED"]), rule: "One deterministic unit of external work with immutable input references and append-only results." }),
  EvidenceCandidate: Object.freeze({ required: Object.freeze(["candidateId", "targetId", "canonicalFieldId", "originalValue", "originalUnit", "normalizedValue", "normalizedUnit", "sourceDocumentId", "pageSection", "context", "applicability", "extractionMethod", "verificationState", "conflictState"]), rule: "Original value/unit and exact provenance are immutable; extraction never directly creates verified production data." }),
  ReviewItem: Object.freeze({ required: Object.freeze(["reviewItemId", "batchId", "targetId", "reason", "severity", "subjectRefs", "status", "createdByRule", "resolution"]), reasons: Object.freeze(["SOURCE-MISMATCH", "PUBLICATION-IDENTITY-AMBIGUITY", "MARKET-AMBIGUITY", "YEAR-AMBIGUITY", "ABS-AMBIGUITY", "TRANSMISSION-AMBIGUITY", "EQUIPMENT-AMBIGUITY", "SAFETY-CRITICAL-CONFLICT", "SOURCE-DISAGREEMENT", "NORMALIZATION-UNCERTAINTY", "PROMOTION-CANDIDATE"]), rule: "Resolution is human-authored where judgment is required and never auto-promotes." })
});

const sourceDiscovery = Object.freeze({ input: Object.freeze(["ResearchTarget", "GapPlan", "known SourceProspects", "source-priority policy", "budget remainder"]), output: "ranked SourceProspect proposals with discovery provenance and UNKNOWN fields preserved", deterministicCore: Object.freeze(["exclude exhausted/duplicate prospects", "apply source-class relevance", "bound requested work", "rank recorded attributes"]), externalAgent: Object.freeze(["inspect bounded official indexes", "propose exact publication identities/paths", "record observations without declaring readiness"]), nonGoal: "No crawler or autonomous browser in MVP." });

const sourcePriorityPolicy = Object.freeze({ order: Object.freeze(["official-service-manual", "official-common-service-manual", "official-owner-manual", "official-service-data-publication", "official-oem-parts-catalogue", "official-technical-bulletin", "official-model-documentation", "high-quality-independent-technical-source", "third-party-mirror"]), rankingFactors: Object.freeze(["Tier A/B authority", "remaining-field relevance", "authenticated applicability", "access feasibility", "prior document richness", "expected marginal practical gap class", "explicit reuse", "duplicate/exhaustion penalty", "safety ambiguity penalty"]), rule: "Order is a default, not a winner: marginal practical relevance and readiness may make an owner manual better than a blocked service manual; Tier C/D cannot establish practical success." });

const readinessGate = Object.freeze({ canonicalRequiredFields: Object.freeze(["authorityKnown", "documentIdentityKnown", "identifierRelationshipResolved", "documentClassKnown", "officialPathKnown", "modelKnown", "yearKnown", "marketKnown", "transmissionKnown", "absEquipmentSufficient", "accessibilityFeasible", "notExhausted", "tierABIndependent"]), result: Object.freeze(["EXECUTION-READY", "AUTHENTICATED-BUT-APPLICABILITY-PARTIAL", "REGISTERED-NOT-REAUTHENTICATED", "ACCESS-BLOCKED", "SOURCE-IDENTITY-PARTIAL", "EXHAUSTED / LOW-MARGINAL-YIELD", "UNKNOWN", "REJECTED-MISMATCH"]), currentAssessment: "Concept is generic, implementation is duplicated/embedded in prospect-reassessment and target-specific result modules; extract one canonical validator/reducer in Factory Foundation." });

const budgetEngine = Object.freeze({ configurableLimits: Object.freeze(["maxPrimaryDocuments", "maxProspectsAuthenticated", "maxFailedAccessAttempts", "maxDuplicateSources", "maxLowYieldDocuments", "maxConsecutiveZeroYieldSources", "maxAuthenticationLoops", "maxAttemptsPerTarget"]), stopEarlyRules: Object.freeze(["stop when target wave completion is reached", "second source requires a recorded remaining-gap rationale", "structural access/applicability blockers exhaust that path", "duplicates never consume yield credit"]), permanentNumbers: null, rationale: "Prior batches used 1–3 documents per target, but evidence is insufficient for universal numeric governance; profiles configure limits and the engine enforces them." });

const normalization = Object.freeze({ ready: Object.freeze(["ml↔L", "psi/bar→kPa", "N·m identity", "literal normalized comparison"]), partial: Object.freeze(["general numeric quantities", "ranges represented elsewhere", "production quantity validation"]), missing: Object.freeze(["maintenance interval/action parser", "clearance condition parser", "part-number normalization", "capacity-condition semantics", "torque component identity", "pressure load/temperature semantics"]), unsafe: Object.freeze(["unit inference", "rounded-value false precision", "averaging", "condition collapse"]), rule: "Unknown parsers preserve original values and create NORMALIZATION-UNCERTAINTY review items." });
const conflictEngine = Object.freeze({ existing: Object.freeze(["same scoped slot differing normalized values becomes CONFLICT", "overlapping layer disagreements remain conflicts", "no automatic winner"]), canonicalStates: Object.freeze(["NO-CONFLICT", "NORMALIZED-MATCH", "APPLICABILITY-SPLIT", "SOURCE-DISAGREEMENT", "UNRESOLVED-CONFLICT"]), missing: Object.freeze(["canonical applicability ordering", "condition-aware comparisons", "source revision supersession", "typed conflict resolution records"]), rule: "Commonness, source count and numerical averaging never resolve conflicts." });
const exhaustion = Object.freeze({ stopReasons: Object.freeze(["SOURCE-EXHAUSTED", "AUTHENTICATION-PATH-EXHAUSTED", "ACCESS-STRUCTURALLY-BLOCKED", "DUPLICATE-SOURCE", "LOW-MARGINAL-YIELD", "NO-READY-PROSPECT", "TARGET-BUDGET-EXHAUSTED"]), input: Object.freeze(["attempt history", "prospect state", "document identity", "yield", "budget", "repeated blocker fingerprint"]), output: "deterministic terminal or next-work transition", currentState: "Terms and examples exist, but no generic reducer/event history." });
const completion = Object.freeze({ states: Object.freeze(["SERVICE-CORE-READY", "SERVICE-CORE-PARTIAL", "RESEARCH-MORE", "BLOCKED", "EXHAUSTED", "REVIEW-REQUIRED"]), distinctions: Object.freeze({ coverageReadiness: "Service Core field state only", sourceReadiness: "whether a prospect may proceed", researchCompletion: "wave terminal state under budget/history", productionReadiness: "separate human-reviewed promotion decision" }), rule: "A target may be EXHAUSTED or BLOCKED below 44/44; DONE is wave-relative, never production-ready by implication." });
const scheduler = Object.freeze({ inputs: Object.freeze(["GapPlan", "ready prospect count", "expected marginal practical class", "prior yield", "duplication", "access friction", "safety risk", "configured batch policy"]), behavior: Object.freeze(["gate before ranking", "rank marginal practical opportunity per bounded document", "stable tie-break by targetId", "reserve configurable manufacturer/adapter concurrency", "never use fame/popularity as a default factor"]), currentGap: "Existing priorityScore rewards verified slots/source count and does not consume prospect readiness, marginal field relevance or work history." });
const checkpointing = Object.freeze({ stableIds: Object.freeze(["batchId", "targetId", "prospectId", "workItemId", "attemptId", "documentId", "candidateId", "reviewItemId", "eventId"]), stateModel: "append-only events plus deterministic reducer and versioned snapshots", guarantees: Object.freeze(["resume after interruption", "skip terminal attempts", "deduplicate documents/evidence", "reproduce gate/budget decisions", "regenerate reports from records"]), currentGap: "Dataset IDs exist for documents/evidence, but no batch/work/attempt/event identity or reducer." });
const adapterDecision = Object.freeze({ pattern: "GENERIC-CORE-PLUS-OPTIONAL-MANUFACTURER-ADAPTERS", coreOwns: Object.freeze(["contracts", "state transitions", "readiness/applicability gates", "budgets", "coverage", "conflicts", "review queue", "checkpointing"]), adaptersMayOwn: Object.freeze(["official index/path templates", "publication-code parsing hints", "authentication navigation hints", "metadata mapping proposals"]), adaptersMustNot: Object.freeze(["declare evidence verified", "infer applicability", "bypass access/readiness gates", "change Service Core", "promote production"]), reason: "Honda/Yamaha/Harley infrastructure differs, but evidence safety semantics do not." });

const scorecard = Object.freeze([
  score("target-generation", "PARTIAL", "Generic catalogue traversal exists; bounded scope contract is missing."), score("source-discovery", "MISSING", "Prompt-driven only."), score("source-registry", "PARTIAL", "Several registries/shapes, no canonical prospect validator."), score("source-authentication", "PARTIAL", "ADR-012 gate exists but is embedded and manually populated."), score("applicability", "PARTIAL", "Fail-closed pieces exist across incompatible shapes."), score("service-core-gap-planning", "PARTIAL", "44-field gaps are ready; history/risk/source relevance are missing."), score("acquisition-budgets", "DESIGN-ONLY", "Per-task budgets are handcrafted."), score("extraction-contract", "PARTIAL", "Candidate validators exist in two schemas."), score("normalization", "PARTIAL", "Small safe conversion table only."), score("conflict-handling", "PARTIAL", "Detection is generic; typed resolution/applicability split missing."), score("coverage", "READY", "Deterministic Service Core coverage exists."), score("yield", "PARTIAL", "Metrics exist per execution but no generic calculator."), score("exhaustion", "DESIGN-ONLY", "States are recorded manually."), score("anti-loop", "DESIGN-ONLY", "MT-09 rule proved semantics, not a reducer."), score("batch-scheduling", "PARTIAL", "Stable queue exists but ranking inputs are inadequate."), score("review-queue", "PARTIAL", "Basic gap queue exists; typed reasons/lifecycle missing."), score("checkpoint-resume", "MISSING", "No work-item event log/reducer."), score("production-isolation", "READY", "Runtime/import and VFR regression tests enforce quarantine."), score("reporting", "PARTIAL", "Deterministic generators exist; every execution still adds custom script."), score("testing", "PARTIAL", "Strong unit/regression coverage; no factory state-transition/scale fixtures yet." )
]);
function score(area, state, evidence) { return Object.freeze({ area, state, evidence }); }

const scaleTests = Object.freeze([
  Object.freeze({ targets: 10, result: "REPRESENTABLE-BUT-MANUAL", bottlenecks: Object.freeze(["prospect creation", "scope/authentication decisions", "custom result/report modules"]), assessment: "Existing batch helpers can calculate gaps/queues, but orchestration still scales linearly with prompts." }),
  Object.freeze({ targets: 25, result: "NOT-OPERATIONALLY-READY", bottlenecks: Object.freeze(["source discovery throughput", "review queue typing", "attempt/budget tracking", "resume", "manufacturer path variation"]), assessment: "Data volume is modest; missing state orchestration and human review triage dominate." }),
  Object.freeze({ targets: 100, result: "BLOCKED-BY-ORCHESTRATION", bottlenecks: Object.freeze(["external request concurrency/rate limits", "checkpoint/restart", "duplicate cross-target documents", "review volume", "repository report churn", "adapter coverage"]), assessment: "Local coverage/conflict logic should scale, but external work and repository-per-target modules would fail operationally." })
]);

const mvpComponents = Object.freeze(["versioned ResearchTarget/SourceProspect/ApplicabilityScope/GapPlan contracts", "canonical validators and readiness/applicability gates", "Batch/WorkItem/Attempt/Event records with deterministic reducer", "catalogue target loader and GapPlan generator", "bounded scheduler/budget policy", "external-result ingestion for authentication/acquisition/extraction", "document/evidence deduplication and conflict integration", "typed review queue", "checkpoint/report regeneration", "hard manual production boundary"]);
const implementationWaves = Object.freeze([
  wave(1, "FACTORY FOUNDATION", "Implement versioned core contracts, validators and one canonical readiness/applicability gate.", Object.freeze(["research/factory/contracts.js", "research/factory/validators.js", "research/factory/readiness-gate.js", "synthetic tests"]), Object.freeze(["ResearchTarget", "SourceProspect", "ApplicabilityScope", "GapPlan"]), "No orchestration, external research, data migration or live prospect execution.", "Synthetic and current-shaped adapters validate fail-closed without changing existing evidence."),
  wave(2, "FACTORY ORCHESTRATOR", "Add Batch/WorkItem/Attempt/Event identities, reducer, budgets, completion and checkpoint snapshots.", Object.freeze(["research/factory/state-machine.js", "research/factory/budgets.js", "research/factory/checkpoint.js"]), Object.freeze(["BatchState", "SourceWorkItem", "AttemptEvent"]), "No external source calls or extraction.", "Interrupted synthetic batches resume byte-deterministically and enforce stop rules."),
  wave(3, "FACTORY WORK ITEMS", "Generate target GapPlans, prospect/authentication/acquisition/extraction work items and typed review items; ingest external findings.", Object.freeze(["research/factory/gap-planner.js", "research/factory/scheduler.js", "research/factory/ingest.js", "research/factory/review-queue.js"]), Object.freeze(["EvidenceCandidate", "ReviewItem", "ExternalFinding"]), "No autonomous browser, crawler, OCR or LLM extractor.", "Multiple synthetic targets produce bounded work and reports without target-specific scripts."),
  wave(4, "FACTORY BATCH PILOT", "Run a small non-production batch through the orchestrator using existing known prospects, including Ténéré BW3-F8197-E0 as an authentication fixture.", Object.freeze(["research/data/factory-batches/*", "factory report generator"]), Object.freeze(["FactoryBatchDefinition", "FactoryBatchResult"]), "No production promotion and no batch-size scaling.", "One interrupted/resumed pilot proves gates, budgets, anti-loop, metrics and review queue."),
  wave(5, "FACTORY SCALE-UP", "Exercise 10 then 25 targets, add only evidence-backed adapters and optimize report/checkpoint volume before considering 100.", Object.freeze(["synthetic/load fixtures", "optional manufacturer adapters"]), Object.freeze(["AdapterResult", "ScaleReport"]), "No claim of autonomous 100-target research.", "25 targets remain deterministic, bounded, resumable and auditable with measured review load.")
]);
function wave(order, name, objective, likelyFiles, contracts, nonGoals, exitCriteria) { return Object.freeze({ order, name, objective, likelyFiles, contracts, migrationImpact: order === 1 ? "none; adapters later preserve current datasets" : "incremental, versioned adapters only", nonGoals, exitCriteria }); }

const tenerePilot = Object.freeze({ catalogVariantKey: "yamaha.tenere-700.gen1", prospectId: "yamaha.tenere700.service.bw3-f8197-e0", publicationCode: "BW3-F8197-E0", role: "FACTORY-PILOT-CANDIDATE", preservedState: "REGISTERED-NOT-REAUTHENTICATED", executionTiming: "FACTORY BATCH PILOT after foundation/orchestrator/work-item waves", reason: "Known unresolved year/market/equipment/access fields exercise the generic gate without inventing a fixture." });
const exactNextTask = Object.freeze({ id: "factory-foundation-contracts-and-gates", count: 1, task: "Implement the bounded Technical Research Factory Foundation: add versioned, manufacturer-neutral ResearchTarget, SourceProspect, ApplicabilityScope and GapPlan contracts with validators, then extract one canonical fail-closed readiness/applicability gate behind compatibility adapters for synthetic/current-shaped fixtures; perform no external research, live data migration, acquisition, extraction or production change.", leverage: "Every later scheduler, work item, checkpoint, budget and review component depends on stable identities/scopes and one gate; implementing orchestration first would encode today's fragmented shapes." });

const audit = Object.freeze({ classification: "ACCEPT-WITH-RISKS", supported: Object.freeze(["core reuses working gap/dedup/conflict/report/isolation modules", "contracts are manufacturer-neutral", "external work is separated from deterministic transitions", "Ténéré is preserved rather than executed", "MVP is incremental and non-production"]), risks: Object.freeze(["two existing candidate/applicability schemas need compatibility adapters", "source discovery remains external and nondeterministic", "normalization breadth is small", "100-target scale is blocked by review and checkpointing until later waves", "contract design must not become a parallel unused schema"]), falsification: Object.freeze(["25 targets still require per-target result scripts after Work Items", "manufacturer adapters begin declaring readiness/evidence", "factory state cannot resume deterministically", "current evidence must be rewritten to adopt contracts", "production imports factory modules"]), conclusion: "The architecture is credible but not yet implemented; acceptance is conditional on foundation adapters reusing current data rather than creating a disconnected framework." });

function buildReport() {
  return Object.freeze({ schemaVersion: "revlog-technical-research-factory-architecture/v1", date: "2026-09-01", genericityStates, readinessStates, decisionClasses, pipelineStages, componentInventory, manualDecisions, contracts, sourceDiscovery, sourcePriorityPolicy, readinessGate, budgetEngine, normalization, conflictEngine, exhaustion, completion, scheduler, checkpointing, adapterDecision, scorecard, scaleTests, mvpComponents, implementationWaves, tenerePilot, exactNextTasks: Object.freeze([exactNextTask]), audit, evidenceAdded: false, researchedNoEvidenceAdded: false, serviceCoreChanged: false, productionChanged: false, runtimeChanged: false, catalogueChanged: false, cloudBackendChanged: false, vfr800ProductionChanged: false, externalMotorcycleResearchPerformed: false });
}

module.exports = Object.freeze({ genericityStates, readinessStates, decisionClasses, pipelineStages, componentInventory, manualDecisions, contracts, sourceDiscovery, sourcePriorityPolicy, readinessGate, budgetEngine, normalization, conflictEngine, exhaustion, completion, scheduler, checkpointing, adapterDecision, scorecard, scaleTests, mvpComponents, implementationWaves, tenerePilot, exactNextTask, audit, buildReport });
